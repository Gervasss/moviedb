import styled from '@emotion/styled';

type GenerosProps = {
  darkMode: boolean;
};

export const Generos = styled.div<GenerosProps>`
  width: 100%;
  height: 90%;
  display: flex;
  align-items: flex-start;
  padding: 0;
`;
